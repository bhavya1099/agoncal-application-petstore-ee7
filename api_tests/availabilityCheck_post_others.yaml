# ********RoostGPT********

# Test generated by RoostGPT for test java-report-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=eb6e4eccc3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.test_API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 2
  payload:
    path: "availabilityCheck_post_others.csv"
    fields: ["subscriberId", "id", "type", "description", "value"]
    skipHeader: true
    delimiter: "^|^"
    
scenarios:
  - name: Non-Success API Responses
    flow:
      - log: "Attempting invalid payload"
      - post:
          url: "/availabilityCheck"
          headers:
            Content-Type: "application/json"
            AUTH_TOKEN: "{{ $processEnvironment.test_AUTH_TOKEN }}"
          json: 
            customer:
              customer:
                subscriberId: "{{ subscriberId }}"
          expect:
            - statusCode: 400
      - think: 2
      - log: "Attempting with invalid endpoint"
      - post:
          url: "/unknownEndpoint"
          headers:
            Content-Type: "application/json"
            AUTH_TOKEN: "{{ $processEnvironment.test_AUTH_TOKEN }}"
          json: 
            customer:
              customer:
                subscriberId: "{{ subscriberId }}"
            address:
              id: "{{ id }}"
              complement:
                complements:
                  - type: "{{ type }}"
                    description: "{{ description }}"
                    value: "{{ value }}"
          expect:
            - statusCode: 404
      - think: 2
      - log: "Attempting incorrect method"
      - get:
          url: "/availabilityCheck"
          headers:
            Content-Type: "application/json"
            AUTH_TOKEN: "{{ $processEnvironment.test_AUTH_TOKEN }}"
          expect:
            - statusCode: 405
      - think: 2
