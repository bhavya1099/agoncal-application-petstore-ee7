# ********RoostGPT********

# Test generated by RoostGPT for test java-report-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=eb6e4eccc3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.test_API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "availabilityCheck_post_success.csv"
    fields:
      - "subscriberId"
      - "id"
      - "complement"
    skipHeader: true
    delimiter: "^|^"
  phases:
    - duration: 60
      arrivalRate: 10
      name: "Warm up"
    - duration: 600
      arrivalRate: 50
      name: "Sustained test"

scenarios:
  - name: "Successful API Requests Scenario"
    flow:
      - log: "Starting availability check..."
      - post:
          url: "/availabilityCheck"
          headers:
            Authorization: "Bearer {{$processEnvironment.test_AUTH_TOKEN}}"
          json:
            customer:                    # Removed duplicated key
              subscriberId: "{{ subscriberId }}"
            address:                     # Removed duplicated key
              id: "{{ id }}"
              complement:                 # Removed duplicated key
                complements:
                  - type: "ES"
                    value: "{{ complement }}"
          capture:
            - json: "$.control.type"
              as: "ControlType"
            - json: "$.resource.inventoryId"
              as: "InventoryId"
            - json: "$.resource.maxBandwidth"
              as: "MaxBandwidth"
            - json: "$.resource.availabilityType"
              as: "AvailabilityType"
            - json: "$.resource.availabilityCode"
              as: "AvailabilityCode"
          expect:
            - statusCode: 200
            - hasProperty: "control"
            - hasProperty: "resource"
            - equals:
                - "{{ ControlType }}"
                - "S"
            - hasHeader: "content-type"
            - headerEquals:
                - content-type
                - "application/json"
      - think: 1
