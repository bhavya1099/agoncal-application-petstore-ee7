# ********RoostGPT********

# Test generated by RoostGPT for test java-report-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=eb6e4eccc3
# 
# 

# ********RoostGPT********
config:
  target: "${processEnvironment.test_API_HOST}"
  environments:
    development:
      target: "${processEnvironment.test_API_HOST}"
  http:
    timeout: 10
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "availabilityCheck_post_success.csv"
    fields:
      - "subscriberId"
      - "addressId"
    skipHeader: true
    delimiter: "^|^"  
  plugins:
    expect: {}

scenarios:
  - name: "POST /availabilityCheck endpoint Success Test Scenario"
    flow:
      - log: "Performing POST /availabilityCheck request"
      - post:
          headers:
            "Content-Type": "application/json"
            "Authorization": "Bearer ${processEnvironment.test_AUTH_TOKEN}"
          url: "/availabilityCheck"
          json:
            customer:
              subscriberId: "${subscriberId}"
            address:
              id: "${addressId}"
          expect:
            - statusCode: 200
            - hasProperty: "control"
            - hasProperty: "resource"
            - equals: ["resource.availabilityType", 1]
      - think: 1
