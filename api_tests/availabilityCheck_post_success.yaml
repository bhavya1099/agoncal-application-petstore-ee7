# ********RoostGPT********

# Test generated by RoostGPT for test java-report-test using AI Type Azure Open AI and AI Model roostgpt-4-32k
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=bc409fdb92
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.testt_API_HOST }}"
  phases:
    - duration: 10
      arrivalRate: 20
  payload:
    path: "availabilityCheck_post_success.csv"
    fields: ["subscriberId", "complements"]
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test Successful Post Requests on AvailabilityCheck API"
    flow:
      - log: "Step 1: Posting a new AvailabilityCheck"
      - post:
          url: "/availabilityCheck"
          auth:
            mode: "default"
            basic:
              username: "{{username}}"
              password: "{{password}}"
          headers:
            content-type: "application/json"
            authorization: "Bearer {{$processEnvironment.testt_AUTH_TOKEN}}"
          json:
            id: "{{ subscriberId }}"
            complement:
              complements: "{{ complements }}"
          expect:
            - statusCode: 200
            - hasProperty: "control"
            - hasProperty: "control.type"
            - equals: 
                - "control.type"
                - "S"
            - hasHeader: "content-type"
            - headerEquals: ["content-type", "application/json"]
      - think: 1
      - log: "Step 2: Verifying details of posted AvailabilityCheck"
      - get:
          url: "/availabilityCheck/{{subscriberId}}"
          headers: 
            authorization: "Bearer {{$processEnvironment.testt_AUTH_TOKEN}}"
          expect: 
            - statusCode: 200
            - hasProperty: "control.type"
            - equals: 
                - "control.type"
                - "S" 
            - hasHeader: "content-type"
            - headerEquals: ["content-type", "application/json"]
      - think: 1
